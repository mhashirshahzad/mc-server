# Python virtual environment (NEVER commit this)
venv/
.venv/
env/
ENV/

# Python cache (NEVER commit)
__pycache__/
*.py[codz]
*$py.class

# Distribution (don't commit)
*.egg-info/
dist/
build/
*.egg

# My GTK app specific
*.log
.cache/
.DS_Store

# Lock file - UNCOMMENT if you want to track it (recommended)
# !requirements-lock.txt

# Flatpak builds (if using)
.flatpak-builder/
build-dir/
